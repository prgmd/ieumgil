# 1. 빌드 및 실행 매뉴얼

저장소를 클론해 **로컬에서 빌드하고 실행**하는 전 과정.

```
STEP 1  저장소 클론
STEP 2  backend/.env 작성
STEP 3  docker compose up -d --build backend      ← backend + postgres + redis
STEP 4  frontend/.env 작성
STEP 5  npm install
STEP 6  npm run dev
```

→ **`http://localhost:5173`** 접속

프론트엔드는 컨테이너에 올리지 않는다. Vite dev server 가 `/api`·`/ws` 를 백엔드로
프록시하므로 브라우저 입장에서 **단일 오리진**이 되고, 이 구조가 배포(nginx 리버스
프록시)와 같은 모양이다.

```
브라우저 ──> Vite dev server (5173)
               ├─ 정적 자원
               ├─ /api  ─┐
               └─ /ws   ─┤ 프록시 (ws: true)
                         ↓
               ┌─ Docker Compose ─────────────────────┐
               │  backend (8080) ── postgres (5432)   │
               │                 └─ redis (6379)      │
               └──────────────────────────────────────┘
```

---

## 1-1. 사용 제품·버전과 설정 값

### 런타임·빌드 도구

| 구분 | 제품 | 버전 | 설정 |
|---|---|---|---|
| JVM (빌드) | Eclipse Temurin **JDK** | 21 (alpine) | Gradle toolchain `languageVersion = 21` |
| JVM (실행) | Eclipse Temurin **JRE** | 21 (alpine) | `JAVA_OPTS=-XX:MaxRAMPercentage=75.0`, `TZ=Asia/Seoul` |
| 빌드 도구 | Gradle Wrapper | 8.12 | `./gradlew bootJar` — 빌드 컨테이너 안에서 실행되므로 호스트에 JDK·Gradle 설치 불필요 |
| 프레임워크 | Spring Boot | 3.4.13 | 프로파일 기본 `local` |
| WAS | 내장 Apache Tomcat | Spring Boot 3.4.13 동봉 | 컨테이너 내부 포트 **8080** 고정, 호스트 포트만 변경 |
| DB | PostgreSQL | 16-alpine | `TZ=Asia/Seoul`, `PGTZ=Asia/Seoul`. 저장소의 Dockerfile 로 함께 빌드 |
| 캐시 | Redis | 7-alpine | `TZ=Asia/Seoul`, 커스텀 `redis.conf`. **비밀번호 없음** |
| 프론트 런타임 | Node.js | **20 이상** | 호스트에 설치 필요 |
| 프론트 번들러 | Vite | 8.x | dev server 포트 **5173** |
| UI | React | 19.x | |
| 컨테이너 | Docker Engine / Compose | 24+ / v2 | |

별도의 외부 웹서버(Apache·nginx)는 로컬 실행에 필요 없다. Vite dev server 가 정적
자원과 API 프록시를 함께 처리한다.

### 주요 라이브러리

**백엔드** — Spring Data JPA, Spring Security, Spring WebSocket(STOMP),
Spring Data Redis, Spring AI 1.1.8 (Anthropic), springdoc-openapi 2.7.0,
jjwt 0.12.6, spring-dotenv 4.0.0

**프론트엔드** — React Router 7, zustand 5, @dnd-kit 6, @stomp/stompjs 7,
axios 1.18, fractional-indexing 4, react-markdown 10 + remark-gfm 4

### 애플리케이션 고정 설정 (`backend/src/main/resources/application.yaml`)

| 항목 | 값 | 비고 |
|---|---|---|
| `spring.jpa.hibernate.ddl-auto` | `none` | 환경변수 `DDL_AUTO` 로 덮어씀 (로컬 `update`) |
| `jwt.access-token-validity` | 1,800,000 ms (30분) | |
| `jwt.refresh-token-validity` | 1,209,600,000 ms (14일) | httpOnly 쿠키 |
| `spring.http.client.connect-timeout` | 5s | 외부 API 공통 |
| `spring.http.client.read-timeout` | 15s | |
| `spring.ai.retry.max-attempts` | 3 | 기본 10회는 과함 |
| `spring.ai.anthropic.base-url` | `https://gms.ssafy.io/gmsapi/api.anthropic.com` | SSAFY GMS 경유 |
| `spring.ai.anthropic.chat.options.model` | `claude-haiku-4-5-20251001` | |
| WebSocket 하트비트 | 10s / 10s | 서버·클라이언트 동일해야 함 |

---

## 1-2. 사전 준비

| 항목 | 확인 |
|---|---|
| Docker Desktop / Engine (Compose v2) | `docker version`, `docker compose version` |
| Node.js 20 이상 | `node -v` |
| Git | `git --version` |
| 카카오 개발자 앱 | REST API 키 발급 + 아래 두 곳 등록 |

### 카카오 콘솔 등록 2곳 — 먼저 해 둔다

[카카오 개발자센터](https://developers.kakao.com) > 내 애플리케이션.
자세한 절차는 [02 문서](./02-외부-서비스.md).

| 메뉴 | 값 | 빠뜨리면 |
|---|---|---|
| 제품 설정 > 카카오 로그인 > Redirect URI | `http://localhost:5173/oauth/kakao/callback` | 로그인 실패 (KOE006) |
| 앱 설정 > 플랫폼 > Web > 사이트 도메인 | `http://localhost:5173` | **지도가 회색으로만 나온다** |

두 번째를 놓치기 쉽다. 로그인은 되는데 지도만 안 뜨면 여기부터 확인한다.

---

# STEP 1. 저장소 클론

```bash
git clone <저장소 URL> ieumgil
cd ieumgil
```

디렉터리 구조

```
ieumgil/
├── backend/          Spring Boot + docker-compose.yml
├── frontend/         React + Vite
├── docs/             설계·배포 문서
└── exec/             포팅 매뉴얼 (이 문서)
```

---

# STEP 2. `backend/.env` 작성

```bash
cd backend
cp .env.example .env
```

편집기로 `.env` 를 열어 값을 채운다.

```bash
# ── PostgreSQL ──────────────────────────────────
POSTGRES_DB=ieumgil
POSTGRES_USER=ieumgil
POSTGRES_PASSWORD=<임의의 비밀번호>

# ── CORS ────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:5173

# ── JWT ─────────────────────────────────────────
# HS256 서명 키, 32자 이상. 짧으면 앱 기동 시 예외가 난다
#   생성: openssl rand -base64 36
JWT_SECRET=<32자 이상>

# ── 카카오 OAuth ────────────────────────────────
KAKAO_CLIENT_ID=<카카오 REST API 키>
KAKAO_CLIENT_SECRET=<Client Secret 을 켰다면. 아니면 빈 값>
KAKAO_REDIRECT_URI=http://localhost:5173/oauth/kakao/callback

# ── 포트 ────────────────────────────────────────
# 컨테이너 내부는 항상 8080. 호스트 포트만 바꾼다
BACKEND_PORT=8080

# ── 스키마 ──────────────────────────────────────
# 엔티티 기준으로 테이블 자동 생성. 빈 DB 로 시작하면 이것만으로 충분하다
DDL_AUTO=update

# ── refresh 토큰 쿠키 ───────────────────────────
# 로컬은 HTTP 이므로 false. HTTPS 배포에서만 true
AUTH_REFRESH_COOKIE_SECURE=false
AUTH_REFRESH_COOKIE_SAME_SITE=Lax

# ── 외부 API 키 (비어 있어도 기동은 된다) ────────
# 해당 기능을 실제로 호출할 때 502 가 난다
KAKAO_REST_API_KEY=<장소 검색·길찾기. 카카오 REST API 키>
ODSAY_API_KEY=<대중교통 경로>
TOUR_API_KEY=<축제 정보. "Decoding" 값을 넣을 것>
GMS_API_KEY=<AI 챗봇>
OPINET_API_KEY=<유가. 비우면 상수 유가로 대체되며 에러가 아니다>
```

### 필수 5개

`POSTGRES_*`, `CORS_ALLOWED_ORIGINS`, `JWT_SECRET`, `KAKAO_CLIENT_ID`,
`KAKAO_REDIRECT_URI` 는 비어 있으면 **컨테이너가 뜨지 않는다.**
나머지 외부 API 키는 비워도 기동되고, 그 기능을 쓸 때만 502 가 난다.

### 주의

- **비밀번호에 `#` 을 쓰지 않는다.** `.env` 에서 주석 시작으로 잘린다.
- `TOUR_API_KEY` 는 공공데이터포털의 **"Decoding" 값**을 넣는다. "Encoding" 값을
  넣으면 클라이언트가 한 번 더 인코딩해 **이중 인코딩으로 401** 이 난다.
- `KAKAO_CLIENT_ID` 와 `KAKAO_REST_API_KEY` 는 **같은 값**이다. 카카오 OAuth 가
  REST API 키를 `client_id` 로 쓰기 때문인데, 용도가 달라 별도 변수로 두었다.

---

# STEP 3. 백엔드 스택 기동

```bash
# backend/ 디렉터리에서
docker compose up -d --build backend
```

`backend` 하나만 지정해도 `depends_on` 에 따라 **postgres·redis 가 함께 뜬다.**
PostgreSQL·Redis 도 이 저장소의 Dockerfile 로 빌드된다(타임존, 초기화 스크립트,
`redis.conf` 포함).

첫 실행은 Gradle 의존성을 받느라 **5~10분** 걸릴 수 있다. 이후 빌드는 캐시가 걸려
훨씬 빠르다.

### 확인

```bash
docker compose ps
# ieumgil-backend / ieumgil-postgres / ieumgil-redis 가 Up (healthy)

docker compose logs -f backend
# "Started BackendApplication in ..." 확인 후 Ctrl+C

curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8080/api/projects
# → 401 이면 정상 기동 (인증 없이 호출했으므로)
```

| 대상 | 주소 |
|---|---|
| 백엔드 API | `http://localhost:8080/api` |
| Swagger UI | `http://localhost:8080/swagger-ui.html` |
| PostgreSQL | `localhost:5432` (127.0.0.1 바인딩) |
| Redis | `localhost:6379` (127.0.0.1 바인딩) |

---

# STEP 4. `frontend/.env` 작성

```bash
cd ../frontend
cp .env.example .env      # 없으면 새로 작성
```

```bash
# 카카오 개발자센터 > 내 애플리케이션 > 앱 키 > REST API 키
VITE_KAKAO_REST_API_KEY=<카카오 REST API 키>

# 카카오 콘솔에 등록한 Redirect URI 와 동일해야 한다
VITE_KAKAO_REDIRECT_URI=http://localhost:5173/oauth/kakao/callback

# 절대주소가 아니라 상대경로. dev server 의 /api 프록시를 타게 되고,
# 브라우저가 보기엔 같은 오리진이라 refreshToken 쿠키가 정상 저장된다
VITE_API_BASE_URL=/api

# /api·/ws 요청을 실제로 중계할 백엔드 주소 (vite.config.js 의 proxy target)
VITE_API_PROXY_TARGET=http://localhost:8080
```

### ⚠️ `VITE_API_PROXY_TARGET` 오타 주의

`localhost` 를 `localhst` 처럼 한 글자만 틀려도, **브라우저에서는 요청이 나간 것처럼
보이는데 백엔드 로그에는 아무것도 안 찍힌다.** dev server 가 존재하지 않는 호스트로
중계하려다 실패하기 때문이다.

이 경우 `npm run dev` 터미널에 아래가 찍혀 있다.

```
[vite] http proxy error: /api/auth/login/kakao
Error: getaddrinfo ENOTFOUND localhst
```

`STEP 2` 에서 `BACKEND_PORT` 를 8080 이 아닌 값으로 바꿨다면 여기 포트도 함께 맞춘다.

### ⚠️ `VITE_*` 는 빌드 시점 값이다

Vite 는 `VITE_*` 를 **번들 소스에 문자열로 치환해 넣는다.** 런타임 환경변수가
아니므로 `.env` 를 고쳤으면 **dev server 를 재시작**해야 반영된다.

---

# STEP 5. 의존성 설치

```bash
# frontend/ 디렉터리에서
npm install
```

---

# STEP 6. 프론트엔드 실행

```bash
npm run dev
```

```
  VITE v8.x.x  ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

브라우저에서 **`http://localhost:5173`** 접속.

### 동작 확인

- [ ] 랜딩 화면이 뜬다
- [ ] **[카카오로 시작하기]** → 카카오 인증 → 개인 페이지 진입
- [ ] 그룹·프로젝트를 만들고 대시보드 진입
- [ ] 후보 블록을 타임라인으로 드래그
- [ ] 사이드 지도가 렌더링된다
- [ ] DevTools > Network > WS 탭에 `/ws` 가 **101 Switching Protocols**

---

## 종료

```bash
cd backend
docker compose stop        # 컨테이너만 정지 (데이터 유지)
docker compose down        # 컨테이너 제거 (볼륨은 남으므로 데이터는 유지)

# 데이터까지 완전히 지우려면
docker volume rm ieumgil_postgres-data ieumgil_redis-data
```

---

## 1-3. 실행 시 특이사항 ★

### ① 스키마는 자동 생성된다 — 신규 설치에 마이그레이션은 불필요

`DDL_AUTO=update` 로 기동하면 엔티티 기준으로 테이블이 만들어진다.
`backend/docker/postgres/migration/*.sql` 은 **이미 운영 중인 DB 를 새 버전으로
이관할 때** 쓰는 것이라, 빈 DB 로 시작하는 로컬 실행에는 필요 없다.

인덱스는 Hibernate 가 만들지 않으므로, 데이터를 많이 넣고 성능을 볼 때만
`001`·`002` 를 추가로 돌린다. → [03 문서](./03-DB-덤프.md)

### ② 카카오 Redirect URI 는 세 곳이 글자까지 같아야 한다

| 위치 | 값 |
|---|---|
| 카카오 콘솔 | `http://localhost:5173/oauth/kakao/callback` |
| `backend/.env` 의 `KAKAO_REDIRECT_URI` | 동일 |
| `frontend/.env` 의 `VITE_KAKAO_REDIRECT_URI` | 동일 |

끝 슬래시 하나만 달라도 `KOE006` 으로 로그인이 실패한다.

### ③ 카카오 지도는 도메인 등록이 필요하다

카카오 콘솔 > 앱 설정 > 플랫폼 > Web > 사이트 도메인에 `http://localhost:5173` 을
등록해야 지도가 렌더링된다. 없으면 **로그인은 되는데 지도만 회색**으로 나오고,
콘솔에도 뚜렷한 에러가 남지 않아 원인을 찾기 어렵다.

### ④ 음성 채팅은 `localhost` 로 접속해야 동작한다

`getUserMedia` 는 **보안 컨텍스트(HTTPS 또는 localhost)** 에서만 허용된다.

| 접속 주소 | 음성 |
|---|---|
| `http://localhost:5173` | ⭕ 동작 |
| `http://<내부IP>:5173` | ❌ 마이크 접근 불가 |
| `https://<도메인>` | ⭕ 동작 |

같은 이유로 초대 링크 복사(`navigator.clipboard`)도 IP 접속에서는 동작하지 않는다.
**다른 기기에서 접속해 시연하려면 HTTPS 가 필요하다.**

### ⑤ 실시간 협업 테스트에는 계정 두 개가 필요하다

접속 배지·라이브 커서·편집 배지는 `memberId` 기준이라 **같은 계정의 두 탭은 서로를
"남"으로 보지 않는다.** 일반 창 + 시크릿 창에 **서로 다른 카카오 계정**으로
로그인해야 실시간 동작을 확인할 수 있다.

### ⑥ 포트 충돌

| 포트 | 용도 | 충돌 시 |
|---|---|---|
| 8080 | 백엔드 | `backend/.env` 의 `BACKEND_PORT` 변경 + `VITE_API_PROXY_TARGET` 을 같이 수정 |
| 5173 | Vite dev server | Vite 가 자동으로 5174 등으로 옮긴다. **이때 카카오 Redirect URI 세 곳도 함께 바꿔야 한다** |
| 5432 / 6379 | PostgreSQL / Redis | 호스트에 이미 떠 있으면 `backend/docker-compose.yml` 의 ports 매핑 수정 |

### ⑦ 포트는 `127.0.0.1` 에만 바인딩되어 있다

PostgreSQL·Redis 는 `127.0.0.1:5432`, `127.0.0.1:6379` 로 묶여 있다.
**`0.0.0.0` 으로 바꾸지 않는다** — 특히 Redis 는 비밀번호가 없어서 무인증 노출 시
원격 코드 실행까지 이어진다. 원격 조회가 필요하면 SSH 터널을 쓴다.

### ⑧ 메모리

`mem_limit` 이 걸려 있다(backend 1g / postgres 512m / redis 256m). 백엔드는
`-XX:MaxRAMPercentage=75` 라 제한이 없으면 **호스트 전체의 75%** 를 힙 상한으로
잡는다. 제한을 풀지 않는다.

### ⑨ 알려진 제약

| 항목 | 내용 |
|---|---|
| TURN 서버 없음 | 공개 STUN 만 사용. 서로 다른 NAT 뒤의 두 사용자 간 음성이 연결되지 않을 수 있다 |
| 오프라인 편집 큐잉 없음 | 네트워크 단절 중 편집은 큐에 쌓이지 않고, 재연결 시 서버 상태로 재동기화 |
| 모바일 | 읽기 전용 (편집 UI 비노출) |

---

## 1-4. 계정·프로퍼티 정의 파일 목록

민감 정보가 들어가는 파일. **`.env` 계열은 전부 `.gitignore` 로 제외되어 커밋되지
않으며, 템플릿(`.example`)만 저장소에 있다.**

| 파일 | 커밋 | 내용 |
|---|---|---|
| `backend/.env` | ❌ 제외 | DB 계정·비밀번호, `JWT_SECRET`, 카카오 OAuth, 외부 API 키 전부 |
| `backend/.env.example` | ✅ | 위 항목의 빈 템플릿 + 설명 |
| `frontend/.env` | ❌ 제외 | `VITE_KAKAO_REST_API_KEY`, `VITE_KAKAO_REDIRECT_URI`, `VITE_API_BASE_URL`, `VITE_API_PROXY_TARGET` |
| `backend/src/main/resources/application.yaml` | ✅ | **비밀값 없음.** 전부 `${ENV}` 참조. 타임아웃·토큰 유효기간 등 고정 설정만 |
| `backend/docker-compose.yml` | ✅ | 값은 `${ENV}` 참조 |
| `backend/docker/redis/redis.conf` | ✅ | Redis 설정. **비밀번호 미설정** — 외부 바인딩 금지 |
| `frontend/src/features/dashboard/map/addressLookup.js` | ✅ | **카카오 지도 JS 키가 상수로 하드코딩되어 있다** (아래) |

### 하드코딩된 키에 대한 설명

`addressLookup.js` 의 카카오 **JS 키**는 지도 SDK 렌더링용이다. JS 키는 도메인
등록으로 사용처가 제한되며(카카오 콘솔 > 플랫폼 > Web > 사이트 도메인), 브라우저에
노출되는 것이 정상적인 사용 방식이다.

다만 현재 **로그인용 REST 키도 번들에 함께 들어간다**(`VITE_KAKAO_REST_API_KEY`).
SPA 에서 OAuth 인가 URL 을 만들려면 불가피하지만, 이 키는 REST 호출에도 쓸 수 있어
**할당량 도용 위험**이 있다. 개선 방향은 지도·장소용 앱을 분리해 JS 키만 프론트에
두는 것이다. 단 **OAuth 는 기존 앱을 유지해야 한다** — 카카오는 앱마다 `kakaoId` 가
달라, 앱을 옮기면 기존 회원이 전부 신규 가입자가 된다.

### 계정 정보

| 대상 | 계정 | 위치 |
|---|---|---|
| PostgreSQL | `POSTGRES_USER` / `POSTGRES_PASSWORD` | `backend/.env` |
| Redis | 인증 없음 | — (127.0.0.1 바인딩으로만 보호) |
| 서비스 로그인 | **카카오 소셜 로그인 전용.** 자체 회원가입·비밀번호 없음 | — |
| 관리자 계정 | 없음 (관리자 기능 미구현) | — |

---

## 1-5. 문제 해결

| 증상 | 먼저 볼 곳 |
|---|---|
| **요청은 나가는데 백엔드 로그가 안 찍힘** | `VITE_API_PROXY_TARGET` 오타·포트 불일치. `npm run dev` 터미널의 proxy error 확인 |
| 컨테이너가 안 뜸 | `JWT_SECRET` 32자 미만, 필수 5개 누락 |
| 로그인 실패 (KOE006) | Redirect URI 세 곳이 글자까지 같은지 |
| 지도만 회색 | 카카오 콘솔 > 플랫폼 > Web > 사이트 도메인 |
| 마이크가 안 잡힘 | `localhost` 로 접속했는지 (IP 접속은 불가) |
| 실시간이 상대 화면에 안 보임 | **다른 계정**으로 로그인했는지 (같은 계정 두 탭은 서로를 남으로 안 봄) |
| `.env` 를 고쳤는데 반영이 안 됨 | 프론트는 dev server 재시작, 백엔드는 `docker compose up -d` |
| 챗봇·교통·축제가 502 | 해당 외부 API 키 미설정 ([02 문서](./02-외부-서비스.md)) |

---

## 부록. EC2 배포 (요약)

운영 배포는 **이미지를 로컬에서 굽고 EC2 가 pull** 하는 방식이다. EC2 에는 소스를
클론하지 않고 `docker-compose.yml` 과 `.env` 두 파일만 둔다.

```
브라우저 ──HTTPS(443)──> nginx (EC2 호스트, Let's Encrypt)
                            ├─ /      → 127.0.0.1:5173   frontend
                            ├─ /api/  → 127.0.0.1:8081   backend
                            └─ /ws    → 127.0.0.1:8081   backend (WebSocket)
```

```bash
# 로컬 PC — 이미지 빌드 후 레지스트리에 push
cd frontend
VITE_KAKAO_REDIRECT_URI=https://<도메인>/oauth/kakao/callback \
  FRONTEND_IMAGE_REPO=<계정>/ieumgil-frontend IMAGE_TAG=<태그> docker compose build
docker push <계정>/ieumgil-frontend:<태그>

cd ../backend
docker build --platform linux/amd64 -t <계정>/ieumgil-docker:<태그> .
docker push <계정>/ieumgil-docker:<태그>
```

```bash
# EC2 — 태그만 바꿔 받아 켠다
cd ~/ieumgil
vi .env                          # BACKEND_TAG / FRONTEND_TAG
docker compose pull && docker compose up -d
```

### 로컬과 다른 점 세 가지

| 항목 | 로컬 | 배포 |
|---|---|---|
| 프론트엔드 | Vite dev server | **컨테이너(nginx + 정적 번들)** |
| `/api`·`/ws` 프록시 | Vite dev server | **호스트 nginx** |
| HTTPS | 불필요 (localhost 는 보안 컨텍스트) | **필수** — 음성·클립보드가 HTTPS 를 요구 |

### 배포에서 특히 주의할 것

- **nginx `/ws` 에 Upgrade 헤더 3줄이 없으면** 실시간 기능 전체가 에러 없이 죽는다
- **`VITE_*` 는 이미지에 구워진다** — 도메인을 바꾸려면 이미지를 다시 굽는다
- **`DDL_AUTO=update` 여도 데이터 마이그레이션은 수동**이다 (특히 006)
- compose 의 `name:` 이 기존과 다르면 **빈 DB 가 새로 생긴다**

전체 절차·nginx 설정 전문·검증 체크리스트는
[`docs/deploy.md`](../docs/deploy.md) 와 [`docs/deploy-checklist.md`](../docs/deploy-checklist.md) 에 있다.
