# 로컬 개발용 Redis 설정
# refresh token 저장소로 쓰이므로, 재시작해도 로그인이 풀리지 않도록 AOF를 켠다.

bind 0.0.0.0
protected-mode no
port 6379

appendonly yes
appendfsync everysec
dir /data

# refresh token이 만료 전에 밀려나면 안 되므로 임의 축출 금지
maxmemory-policy noeviction

loglevel notice
