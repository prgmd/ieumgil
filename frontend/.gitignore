# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# 개발용 .env 는 배포 빌드에도 그대로 쓴다 — 다른 값(VITE_KAKAO_REDIRECT_URI)만
# 빌드 명령에서 덮어쓴다. 별도의 배포용 env 파일을 두지 않는다.
.env
.env.prod